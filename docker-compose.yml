version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: tz-magazine-db
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: mirbek777389866
      POSTGRES_DB: shop_db
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: